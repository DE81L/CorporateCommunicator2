NODE_ENV=production
# API и UI на одном порту
PORT=5173

# фронт во время сборки «зашьёт» сюда адрес API
VITE_API_URL=http://localhost:5173

# если юзаем python-ws на 8001 — не трогаем
VITE_WS_URL=ws://localhost:8001/ws
