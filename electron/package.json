{
  "name": "electron",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --loader ts-node/esm -r tsconfig-paths/register src/main.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "electron": "34.5.5"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0"
  }
}
