# Запуск тестов

Все unit- и e2e-тесты находятся в каталоге `tests` и выполняются с помощью Jest.

```bash
pnpm exec jest
```

Перед запуском убедитесь, что установлены все dev-зависимости командой `pnpm install`.
