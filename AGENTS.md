Каждое твое действие должно записываться в отдельный файл в docs.

Комментарии в коде пиши только на русском языке.
Добавляй переводы для всех новых элементов интерфейса.
Всегда читай и дополняй документацию в папке docs, описывая любые изменения проекта.

Всегда сверяйся с таблицей `docs/features-status-table.md`. Проверяй, какие пункты реализованы, а какие ещё нет. Удалять строки из таблицы можно только по моему прямому указанию и только после вопроса «протестировал ли я новую функцию». Если я подтверждаю тестирование, пункт может быть удалён.

